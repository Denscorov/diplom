<phone:PhoneApplicationPage
    x:Class="Testing_3.View.TestingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="False">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Style="{StaticResource TitleBlock}">
            <TextBlock Text="{Binding Source={StaticResource LocalizedStrings}, Path=LocalizedResources.ApplicationTitle}" Style="{StaticResource PhoneTextNormalStyle}" Foreground="#fff"/>
            <!--<TextBlock Text="{Binding Source={StaticResource LocalizedStrings} ,Path=LocalizedResources.ThemesList}" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}" Foreground="#fff"/>-->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Padding="20 0" Foreground="#fff">
                    <Run Text="Питання: "/>
                    <Run x:Name="QuestionNumber" Text="0"/>
                </TextBlock>
                <TextBlock Padding="20 0" Foreground="#fff">
                    <Run Text="Вірно: "/>
                    <Run x:Name="TrueQuestionNumber" Text="0"/>
                </TextBlock>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Name="questionText" Text="questionText" Grid.Row="0" TextWrapping="Wrap"/>
            <ListBox Grid.Row="1" Name="Answers" />
            <Button Grid.Row="2" Content="Наступне" Name="NextQuestion"/>

        </Grid>
            
    </Grid>

</phone:PhoneApplicationPage>